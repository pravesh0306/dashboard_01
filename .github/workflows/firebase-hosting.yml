name: Deploy to Firebase Hosting (DISABLED)

# Temporarily disabled - Firebase service account needs setup
# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

on: workflow_dispatch  # Only manual trigger

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm install
      
    - name: Build project (if needed)
      run: |
        # Add any build commands if needed
        echo "Project ready for deployment"
    
    - name: Deploy to Firebase
      uses: FirebaseExtended/action-hosting-deploy@v0
      with:
        repoToken: ${{ secrets.GITHUB_TOKEN }}
        firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TEST_FILEUPLOAD_BBF7E }}
        channelId: live
        projectId: test-fileupload-bbf7e
